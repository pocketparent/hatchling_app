/**
 * Hatchling App Theme Index
 * 
 * Central export for all theme elements
 */

import colors from './colors';
import typography from './typography';
import spacing from './spacing';

export default {
  colors,
  typography,
  spacing,
};
